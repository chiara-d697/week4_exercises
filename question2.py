# In the first example, the variable is interpreted as a string,
# so the length reflects how many characters the string has.
# In the second example, 'tup' is a tuple, due to the presence of the comma.
# Len therefore displays one, as there is one item in the tuple.